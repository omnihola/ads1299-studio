# Build
/build/
/Testing/
/cmake-build-*/
*.o
*.obj
*.a
*.so
*.dylib
# IDE
/.idea/
/.vscode/
*.user
# macOS
.DS_Store
# Recordings / data
/recordings/
*.bdf
*.edf

# MMB0 firmware image (TI-licensed; user supplies their own copy —
# see Mmb0Bootloader::defaultCandidates)
/firmware/

# SDD scratch
/.superpowers/

# RE reference (TI source kept locally for interop; review licensing before publishing)

# TI reverse-engineering reference + PDK firmware source mirror (kept local
# only; do not commit upstream GPL/CPL source). The firmware fixes derived
# from this tree live as a portable patch under firmware-patches/ instead.
/reference/

#include "core/acquisition/IDataSource.h"

namespace studio {

// Moc-generated vtable and signal implementations go here.
// IDataSource is abstract, so we only need this .cpp file to exist
// so the linker can find moc-generated definitions.

}  // namespace studio
